<UserControl x:Class="BMHRI.WCS.Server.UserControls.TextDescription"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BMHRI.WCS.Server.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="50">
    <Border BorderThickness="0.5">
        <Grid x:Name="g" >
            <Viewbox  VerticalAlignment="Center" HorizontalAlignment="Left" >
                <Label x:Name="DeviceIDLabel" Content="抛丸区出库口3134" />
            </Viewbox>
        </Grid>
    </Border>
</UserControl>
